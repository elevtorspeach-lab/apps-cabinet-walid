{
  "name": "applicationversion1",
  "version": "1.0.0",
  "description": "applicationversion1 offline desktop app",
  "main": "main.js",
  "author": "applicationversion1",
  "private": true,
  "scripts": {
    "start": "electron .",
    "sync:web-assets": "node scripts/sync-offline-web.mjs",
    "verify:web-assets": "node scripts/validate-offline-sync.mjs",
    "build:mac": "npm run sync:web-assets && npm run verify:web-assets && electron-builder --mac --x64",
    "build:win": "npm run sync:web-assets && npm run verify:web-assets && electron-builder --win portable --x64",
    "build:win:portable": "npm run sync:web-assets && npm run verify:web-assets && electron-builder --win portable --x64"
  },
  "devDependencies": {
    "electron": "^30.5.1",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.applicationversion1.offline",
    "productName": "applicationversion1",
    "icon": "build/icon.png",
    "files": [
      "main.js",
      "preload.js",
      "offline-web/**/*"
    ],
    "mac": {
      "target": [
        "dmg"
      ]
    },
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "applicationversion1-${version}.${ext}"
    }
  }
}
